import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard',
};

export { DashboardContainer as default } from '@/subdomains/(private)/(admin)/dashboard/container/dashboard.container';
