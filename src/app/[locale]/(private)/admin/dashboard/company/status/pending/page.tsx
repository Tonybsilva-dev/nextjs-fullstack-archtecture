import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Pending approval',
};

export { TenantPendingContainer as default } from '@/subdomains/(private)/(admin)/dashboard/company/status/pending/container/tenant-approve.container';
