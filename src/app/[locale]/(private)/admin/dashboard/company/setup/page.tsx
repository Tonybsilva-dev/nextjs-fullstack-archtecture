import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Setup company',
};

export { SetupCompanyContainer as default } from '@/subdomains/(private)/(admin)/dashboard/company/setup/container/setup-company.container';
